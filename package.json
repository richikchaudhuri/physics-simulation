{
  "name": "physics-sandbox",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build crates/physics --target web --out-dir ../../src/physics-pkg",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "rollup": "^4.61.1",
    "vite": "^8.0.16",
    "vite-plugin-top-level-await": "^1.6.0",
    "vite-plugin-wasm": "^3.6.0"
  }
}
